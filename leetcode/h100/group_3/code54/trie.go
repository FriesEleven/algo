package code54

/*
实现 Trie（前缀树）

实现 Trie 类：Insert(word) 插入字符串；Search(word) 判断字符串是否完整存在；
StartsWith(prefix) 判断是否存在以指定前缀开头的字符串。
*/
type Trie struct {
}

func Constructor() (trie Trie) {
	return
}

func (t *Trie) Insert(word string) {
}

func (t *Trie) Search(word string) (found bool) {
	return
}

func (t *Trie) StartsWith(prefix string) (found bool) {
	return
}
