package code32

// Node 是带随机指针的链表节点。
type Node struct {
	val    int
	next   *Node
	random *Node
}

/*
随机链表的复制

给定一个链表，每个节点除 next 指针外还有一个可指向任意节点或 nil 的 random 指针。
请构造该链表的深拷贝，使新链表的节点和指针关系与原链表一致且不引用原节点。
*/
func copyRandomList(head *Node) (copiedHead *Node) {
	return
}
